<Page x:Class="yakovleva_pr7.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:yakovleva_pr7.Pages"
      mc:Ignorable="d" 
      Title="RegistrationPage">

    <Grid x:Name="RegPanel" Grid.Column="0" Width="300" Height="250">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Style="{DynamicResource Lable}" Content="Имя:" Grid.Row="0" Grid.Column="0"/>
        <Label Style="{DynamicResource Lable}" Content="Фамилия:" Grid.Row="1" Grid.Column="0"/>
        <Label Style="{DynamicResource Lable}" Content="Отчество:" Grid.Row="2" Grid.Column="0"/>
        <Label Style="{DynamicResource Lable}" Content="Специальность:" Grid.Row="3" Grid.Column="0"/>
        <Label Style="{DynamicResource Lable}" Content="Пароль:" Grid.Row="4" Grid.Column="0"/>
        <Label Style="{DynamicResource Lable}" Content="Подтвердите пароль:" Grid.Row="5" Grid.Column="0"/>

        <TextBox Text="{Binding Name, Mode=TwoWay}" Style="{DynamicResource Textbox}" Grid.Row="0" Grid.Column="1" />
        <TextBox Text="{Binding Surname, Mode=TwoWay}" Style="{DynamicResource Textbox}" Grid.Row="1" Grid.Column="1" />
        <TextBox Text="{Binding Patronimic, Mode=TwoWay}" Style="{DynamicResource Textbox}" Grid.Row="2" Grid.Column="1" />
        <TextBox Text="{Binding Specialization, Mode=TwoWay}" Style="{DynamicResource Textbox}" Grid.Row="3" Grid.Column="1" />
        <TextBox Text="{Binding Password, Mode=OneWayToSource}" Style="{DynamicResource Textbox}" Grid.Row="4" Grid.Column="1"/>
        <TextBox Text="{Binding ConfirmPassword, Mode=OneWayToSource}" Style="{DynamicResource Textbox}" Grid.Row="5" Grid.Column="1"/>

        <Button Name="BackBtn" Grid.Row="6" Grid.Column="0" Content="Назад" Click="BackBtn_Click" Style="{DynamicResource StrokeButton}"/>
        <Button Name="RegBtn" Grid.Column="1" Grid.Row="6" Content="Регистрация" Click="RegBtn_Click" Style="{DynamicResource DefaultButton}"/>
    </Grid>
</Page>
