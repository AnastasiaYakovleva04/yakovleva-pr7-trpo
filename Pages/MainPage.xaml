<Page x:Class="yakovleva_pr7.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:system="clr-namespace:System;assembly=mscorlib"
      xmlns:local="clr-namespace:yakovleva_pr7.Pages"
      xmlns:converters="clr-namespace:yakovleva_pr7.Converters"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      Title="MainPage">

    <Page.Resources>
        <converters:AgeConverter x:Key="AgeConverter"/>
        <converters:AdultConverter x:Key="AdultConverter"/>
    </Page.Resources>

    <StackPanel>
        <Label Content="Прием пациентов" Style="{DynamicResource Title}"/>
        <Border Style="{DynamicResource Bordr}">
            <StackPanel x:Name="DocInfoPanel" Style="{DynamicResource SpecialPanel}" Grid.Row="3" Grid.Column="0">
                <Label Content="ID:" Style="{DynamicResource SecondaryText}"/>
                <Label Content="{Binding Id, Mode=OneWay}" Style="{DynamicResource SecondaryText}"/>
                <Label Content="{Binding Surname, Mode=OneWay}" Style="{DynamicResource SecondaryText}"/>
                <Label Content="{Binding Name, Mode=OneWay}" Style="{DynamicResource SecondaryText}"/>
                <Label Content="{Binding Patronimic, Mode=OneWay}" Style="{DynamicResource SecondaryText}"/>
                <Label Content="{Binding Specialization, Mode=OneWay}" Style="{DynamicResource SecondaryText}"/>
            </StackPanel>
        </Border>

        <StackPanel Style="{DynamicResource Panel}" HorizontalAlignment="Center" Height="40">
            <Button Name="AddPacientBtn" Style="{DynamicResource StrokeButton}" Click="AddPacientBtn_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Добавить "/>
                    <materialDesign:PackIcon Kind="Plus" Width="18" Height="18" Margin="4,0,0,0"/>
                </StackPanel>
            </Button>
            <Button Name="StartAddmissionBtn" Style="{DynamicResource DefaultButton}" Content="Начать прием" Click="StartAddmissionBtn_Click"></Button>
            <Button Name="EditPacientBtn" Style="{DynamicResource StrokeButton}" Content="Изменить" Click="EditPacientBtn_Click"></Button>
            <Button Name="DeletePacientBtn" Style="{DynamicResource DeleteButton}" Click="DeletePacientBtn_Click">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Удалить"/>
                    <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Margin="4,0,0,0"/>
                </StackPanel>
            </Button>
        </StackPanel>
        
        
        <ListView ItemsSource="{Binding Pacients}" SelectedItem="{Binding SelectedPacient}" Style="{DynamicResource Listview}" ItemContainerStyle="{DynamicResource ListviewItem}">
            <ListView.Resources>
                <Style TargetType="ScrollBar" BasedOn="{StaticResource Scrollbar}"/>
            </ListView.Resources>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel x:Name="PacInfoPanel" Grid.Row="1" Grid.RowSpan="3" Grid.Column="1">
                        <StackPanel Orientation="Horizontal">
                            <Label Style="{DynamicResource Lable}" Content="Фамилия:"/>
                            <Label Style="{DynamicResource Lable}" Content="{Binding Surname, Mode=OneWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Style="{DynamicResource Lable}" Content="Имя:"/>
                            <Label Style="{DynamicResource Lable}" Content="{Binding Name, Mode=OneWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Style="{DynamicResource Lable}" Content="Отчество:"/>
                            <Label  Style="{DynamicResource Lable}" Content="{Binding Patronimic, Mode=OneWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Style="{DynamicResource Lable}" Content="Дата рождения:"/>
                            <Label Style="{DynamicResource Lable}" Content="{Binding Birthday, Mode=OneWay}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Style="{DynamicResource Lable}" Content="Номер телефона:"/>
                            <TextBlock Style="{DynamicResource TextBlock}" Text="{Binding PhoneNumber, Mode=OneWay, StringFormat={}{0:8 (###) ### ## ##}}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Content="Возраст:" Style="{DynamicResource Lable}"/>
                            <Label Content="{Binding Birthday, Mode=OneWay, Converter={StaticResource AgeConverter}}" Style="{DynamicResource Lable}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <Label Content="Статус:" Style="{DynamicResource Lable}"/>
                            <Label Content="{Binding Birthday, Mode=OneWay, Converter={StaticResource AdultConverter}}" Style="{DynamicResource Lable}"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Horizontal">
            <StackPanel x:Name="SystemStatusPanel" Style="{DynamicResource Panel}">
                <Label Content="Докторов:" Style="{DynamicResource SecondaryText}"/>
                <Label Content="{Binding DoctorsCount}" Style="{DynamicResource SecondaryText}"/>
            </StackPanel>
            <Label Content="Пациентов:" Style="{DynamicResource SecondaryText}"/>
            <Label Content="{Binding Pacients.Count}" Style="{DynamicResource SecondaryText}"/>
        </StackPanel>
    </StackPanel>
</Page>

